export const BREAK_GAP = 15 * 60 // seconds — schedule gaps larger than this are breaks
export const BUCKET_SEC = 30 // cycle-time distribution bucket width in seconds
export const LIVE_REFRESH_MS = 60 * 1000
export const CURRENT_YEAR = 2026 // FIRST Championship year (cmpYear) for the active season
export const LS_VERSION = 1
export const LS_PREFIX = `ct_ftc_v${LS_VERSION}_`
export const CURRENT_YEAR_ROWS_TTL_MS = 6 * 60 * 60 * 1000
